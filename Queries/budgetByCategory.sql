select BudgetCat,sum(amount) from BigTXView where XclFrmCshFlw not like '%y%' and budgetCat not like "%ayment%" and budgetCat not like "%Income%" and budgetCat not like "%Transfer%" group by BudgetCat order by sum(amount) asc;
