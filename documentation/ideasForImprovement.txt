1. Have a program read the Excel file and update the configuration files automatically.
   (That would be cool!).  The first file that can be used/converted could be the
   DollarsPerDayExpenditures.  
   Not so sure this is a good idea because Excel is bailing out, and we don't want to
       tie ourselves to a product/software that isn't going to be supported in the future.

2. Create a column for normalized description.  This would search for a recognizable word or words in the description that does not include the numbers of the transaction or store number.
(This is hard to do because some descriptions of transactions contain numbers.)

3. Put DollarsPerDayExpenditures and maybe some other configuration files tables inside
the database including:
1) Budget.csv
2) MarkMandatoryMap.csv
3) Categorized.csv
4) (Maybe errors? - probably not though)
5) DollarsPerDayExpenditures.csv
6) allCategories.csv
7) SavingsPercentages.csv (although I don't remember how this one works at this point)
8) SuggestedSavings.csv (although I don't remember how this one works at this point - ditto from above)
9) pieChart.csv (spending percentages)
10) XcldFrmCshFlw.csv

4. Put the range in the budget so user knows what days are being accounted for (or where
it started)
   This is known from the DollarsPerDayExpenditures.csv date on the first line of the file.

5. Put a blob in there for receipts.  (This one is obvious, but I'll probably never do it
because it's really for tax purposes, and I'll never be able to itemize. - I don't spend
enough on other things to itemize.)

6. Specify in the "Budget Last X days" (in the Excel spreadsheet, how many days have been specified
so that the user knows and can use that information if he/she needs to.)
   This is currently hard-coded in the program.  I think it's 31 or 30 days.

7. Specify in "Budget of all Txns" (in the Excel spreadsheet) when the budget started (what the start date of the transactions is)
    (Again, see #8)

8. Put columns headers in files that don't have them.  Like output.csv and output2.csv. (There may be more)

9. Change code such that javadoc doesn't create so many warnings.  (Get rid of all the warnings).

10. Create a GUI for crying out loud.  Spring, sqlite3, hibernate (with sqlite3 dialect), etc. etc.

11. For some reason, the current day isn't uploaded from output.csv into Excel.  I think it's because
    the sql selects the day before, but I haven't looked at the code.  Fix this.
    
12. Put percentage over/under budgeted amount in MonthlyStatus.
    It would be interesting to see what and how much each category contributes
    to the overall savings/overspending for the month.  This wouldn't be that difficult
    to calculate and can be easily added to the .csv file as well.

12.5 If I put the budget in the DB, I can do queries and sums REALLY easy.  With Excel
        it's a continual task because the categories may change, which would change the
        spreadsheet and the formulas would need to be moved.

13. Summary numbers for the Monthly budget would also be interesting:
    (This idea is repeated in another idea - at the time of this writing idea 16)
    1) days since 1st day of month
    2) average days per month (or actual days per month for the month that we're in)
    3) Theoretical time-based ramp/scale (the amount, if spending money linearly, I would have
        spent up to the date of running the program for the current month.
    4) amount over/under the targeted budget for the month
    5) percentage over/under the tageted budget for the month.
        It's would be interesting to see what the biggest contributors are to the overall
        savings/overspending for the month.  This is already shown in dollars, just not
        percentages.
        Large differences may be an indication that the budget needs to be modified, especially
        if it's for multiple months in a row.

        The calculation for this is: difference/(total monthly budgeted amount)
        The total monthly budgeted amount would need to be calculated.
        This would be easier if it was in a table, but can be done w/o being in a table.

14. Recurring functionality. This functionality was implemented before.  The code got deleted from GitHub
    though.  Essentially, this would analyze the past few months and compare each transaction in the
    current month with the previous month(s) to see if there is a matching transaction
    (same amount, same description).
    I'm guessing this could be done quarterly (yearly as well, if there's data?) as well.
    Since I usually start a new TXs2.db file each year, this would be of much use.  Maybe
    I could change the process to keep the DB?
    This could be a recurring transactions report.  Maybe I could make a table, and output it as well?
    The transactions could be marked M for monthly, and Q for Quarterly.  I know I don't pay
    Waste Mangement bills every month.  Granite propane is also every quarter.
    Another part of this could be whether the amount is constant, or is a recurring
    expense that has differing amounts.  I could mark that as well with something like 'C'
    for Constant and 'V' for variable (or 'D' for dynamic?) 
    It would also be nice if I could add a column for 'recurring', OR, mark the transaction as 'R'
    for recurring.  (Maybe in the mandatory column of BigTXView?)  If I do this at the end of the
    Finance.java class/program, I could just add the 'R' to current string in that column. 

    The way this can be done, instead, is to query/list all transctions with the same description
    and then compare the number of transactions returned with the number of months that
    the comparison contains.  (This may be a better way to implement.)  Maybe that's why they
    deleted that version of the finance program because it was a bad implementation.
    It will certainly be much quicker to do it this way.  (They shouldn't have stolen my software
    if they didn't want an implementation like that!)

    So, for example, if there is a transaction in the current month like the following:
    2025-06-08 00:00:00|AT&amp;T FORDDATAPLAN|-20.0300006866455|INTERNET SERVICE|||VisaChaseTXs
    Then a search would be like this:
    select * from BigTXView where description like '%FORDDATA%' and amount > -20.04 and amount < -20.02;

    In order to do this I'll need to find some code that help me find transactions up 
    to and including the last 3 months or last 90 days.  (Quarter will be the farthest spread there is.)
    There is code that exists already that shows getting the first of the month.  I'll just
    have to use that but wind the clock back 90 days.
    Next, I will run a query for the last 90 days.  I will traverse the ResultSet and 
    for every transaction:
      (Maximum confidence)
   	find the date 93 days before the date of the transaction
	MONTHLY
    	query for the amount and description of that transaction from the found date to now
	      If exactly 3 transactions are found mark them as recurring monthly and store them
	      for later.
	QUARTERLY
	      If transaction is found exactly 1 time mark them as recurring quarterly and store them

      (Less confidence)
	MONTHLY
	query for the description and the date specified earlier
	      If there are EXACTLY 3 transactions and the transaction has the same
	      	 DAY OF MONTH (within +/- 4 days),
		 AND the amount is within +/-$50?
	      	 it's got to be recurring even though it doesn't have the same amount.
		 (Like PG&E/NV Energy)
		 store it for later use.
	QUARTERLY
	      If the day is within +/- 4 days and there is exactly 1 transaction
	      (Like GRANITE PROPANE)
	      	 
	What will be stored?  I think I'll create a class for recurring transactions:
	     The class will have these fields/members:
	     	 1) AVERAGE day of purchase. (truncated - to the nearest integer) (Truncated is easier)
		 2) The average amount
		 3) The description of the recurring charge.
		 4) Found date (The date the recurrence was first found)
		 5) The last/latest a recurrence was verified (again).  (update of table if it's
		    found again.)
	To keep track of recurring charges (after a lot of searches are being done throughout
	   the year) I think I'll keep a table of them.

Monthly Examples (2):
(Example Queried for day of 2025-06-20)
sqlite> select * from BigTXView where description like '%FORDDATA%' and amount < -20.02 and amount > -20.04 and transactiondate > '2025-03-19 00:00:00'
   ...> ;
2025-04-09 00:00:00|AT&amp;T FORDDATAPLAN|-20.0300006866455|INTERNET SERVICE|||VisaChaseTXs
2025-05-09 00:00:00|AT&amp;T FORDDATAPLAN|-20.0300006866455|INTERNET SERVICE|||VisaChaseTXs
2025-06-08 00:00:00|AT&amp;T FORDDATAPLAN|-20.0300006866455|INTERNET SERVICE|||VisaChaseTXs

sqlite> select * from BigTXView where description like 'NV Ener%' and transactiondate > '2025-03-19 00:00:00';
2025-04-03 00:00:00|NV ENERGY NORTH SPPC PYMT|-69.5400009155273|UTILITIES : ELECTRICITY||M|CheckingStarOneTXs
2025-05-02 00:00:00|NV ENERGY NORTH SPPC PYMT|-65.9899978637695|UTILITIES : ELECTRICITY||M|CheckingStarOneTXs
2025-06-02 00:00:00|NV ENERGY NORTH SPPC PYMT|-74.7900009155273|UTILITIES : ELECTRICITY||M|CheckingStarOneTXs

Quarterly Example:
sqlite> select * from BigTXView where description like 'WM.COM' and amount > -66.70 and amount < -66.69 and transactiondate > '2025-03-19 00:00:00';
2025-04-24 00:00:00|WM.COM|-66.6999969482422|UTILITIES|||VisaChaseTXs

    It looks like I'll have to round down and truncate for the boundaries of the amount.

15. I currently have a "Mandatory Calculations" worksheet in the excel file.
    However, it's pretty messed up and relies on Excel vlookup.  It would be nice to have
    this done by the Java program.  This would help in determining future expenditures for
    budget status analysis.
    I had the recurring functionality before. This could be part of this functionality.

16. Add overall (total) budget amount and actual spent amount (total for all categories)
    to monthly budget
    reporting.  This way, if a user wants to spend to match the overall budget he or she can.
    (i.e. if they underspend in one category this likely gives them the freedom to spend 
    more in another).

    Also, it would be nice if an automatic calculation of whether I would be -over/+under budget
    if I stopped ALL spending at this point considering future TXs.  (Requires Mandatory/recurring
    TXs functionality).  Calculations:  daysSinceFirstOfMonth (programming logic),
    averageDaysPerMonth (constant),
    totalBudgetedAmount SUM(amounts of each budgetCategory)
    actualSpentAmount SUM(Each TX since beginning of month)
    linearExtrapolationEstimatedAmountSpent (daysSinceFirstOfMonth/avgDaysPerMonth*totalBudgetedAmount)
    amount -over/+under linearExtrapolationEstimatedAmountSpent
    percentageDifferenceOfAmountOverUnderBudgetedAmount = 100* (linearExtrapolationEstimatedAmountSpent - actualSpentAmount) / linearExtrapolationEstimatedAmountSpent

    Future TXs needed.  This would be mandatory and recurring (those sets of TXs may overlap)
    
    If I stopped spending TOTALLY at this point,
    would I be within budget considering future TXs?
    wInBudget
    I would have spent this much considering future TXs (adding in future TXs): actualSpentAmount + ABS(SUM(future TXs))

    I would be (-over/+under) the budget amount by: 
    amountOverUnderBudget = wInBudget - totalBudgetedAmount


----------------------------------------------------------------

Not new ideas, but existing functionality I need to get working again.

1) Budget all Txns.  Get working.  (Right now the Allowed spending column looks wrong)
   (There are a lot of '0's)
2) Get mandatory calcs going
   It needs the data file generated imported.  The spreadsheet needs to be cleaned up.
3) Flat needs needs to be worked on and filled out
4) Suggested Savings is actually an output of the program.  But, I need to understand it and get it working as well.
5) SuggestedDeductionsNTransfers (This is really if I'm saving money)  (Once I get stable, maybe I could add to this.)

----------------------------------------------------------------

Completed ideas (existing functionality I need to figure out):

Debt Projection.  This is a HUGE one.  I should give this one priority.

(was) 2. Instead of just using Descriptions for Mandatory status, how about using a category?
      Instead of creating a new format to read, a new file was created that had just
      	      the category, and the Mandatory type (I.E. (T)ithe, (M)andatory... etc.)
	      
----------------------------------------------------------------

Completed ideas (new functionality or modification to existing functionality) (also rejected ideas):

- Discover and ask users about whether certain transactions that haven't been seen before are categorized properly.
- Do a query that looks for Transactions without categories and alert user.

-- Both of these were implemented.  Essentially if a category inside the database (or read in
files from banks/cards) is not recognized from the categories list
(Categorize/Categorized.csv), the transaction is listed inside of the errors.csv
file.
-- Also, if there is no category at all, it is listed inside of the errors.csv file.

- I created a script that turns DollarsPerDayExpenditures.csv into a MONTHLY budget.
  This will (or helps) with comparing the budget (Dollars...) to the current spending
  for the last N days. (like around (or less than) a month).  This comparison, referred to in the last
  2 sentences, helps users know whether they can spend on something, like a trip to
  Dayton, considering their budget.

- Explanation of Budget.csv
  This file represents the budget since the date specified in DollarsPerDayExpenditures.csv
  It is SUPPOSED to say how the budget is going given ALL the data from the point specified in DollarsPerDayExpenditures.csv

- I need a script that takes MONTHLY budget data (like DollarsPerDay... but for monthly data) and turns it into
    DollarsPerDayExpenditures.csv.  This way I can take a Monthly budget and use it in this program
    to generate the Budget.csv data. (This is done... MnthlyDrsPD.pl) (converts DollarsPerMonth.csv
       and turns into DollarsPerDayExpenditures.csv - if redirected to that file. (uses stdout)).

- Fix the fact that pieChart uses the last 40 days, instead of last 30 days (or since the first of the month).

- Get rid of all "TRANSFER/transfer" items from pieChart.csv (and any positive values?).

- This is actually done by the long term budget already.  If you establish a budget item, and watch how many
       Days it is till you can spend on this, it'll virtually allocate money for you.

       This really doesn't take into account how much money you've overspent in a month.  But, hey... beggars can't
       be choosers!  Because it's long term, hopefully things will balance out, unless you REALLY
       can't control your spending.
       
  It would also be nice to create an imaginary side fund that deposits imaginary money into an imaginary "want" fund
    that will eventually be used to buy something that is "wanted".  This way, money can be set aside for later purchases
    if the amount is too great for a single month to accomodate.  This may be a bit complicated though.  I'm not really sure
    how it would work.  Potentially, Star One could be used to deposit money into an account like this and a "transfer"
    can be used to fund it.  Also, any unused budget money could go into this account to save for later use if not
    used during the month.  I think this is what SUGGESTEDSAVINGS is for.

- Another nice thing to have would be to be able to compare current actual spending to the MONTHLY budget.    
    The end result should look like pieChart.csv.  This way, the results can be
    subtracted _with_ (not from) pieChart.csv for a comparison and detail on what I can spend on without going over budget.

    It may be nice to have a spreadsheet to dictate how much I have to spend in each category.

    The other option is to have a program subtract pieChart.csv from DollarsPerMonth.csv to see how much I have to spend
    in each category.
       To implement this, DollarsPerMonth.csv is compared to pieChart.csv.Suggested Savings can be taken out later (or accounted for)
       but for now, I just want it to work without that.  (See #14.)
       Unfortunately, the same categories may not be available in each file.  So, a comparison of like categories needs to be
       		      done. (I don't want to do this in Perl.)
    DollarsPerMonth.csv probably will need to be edited manually.

    (Same idea written differently)
    The Monthly Budget Status is different (Initially implemented using PERL).
    The Monthly Budget is a "Baby Steps" (each "step" being a month) approach that allows the user to use the
    	BEGINNING of the 40 day period (or whatever specified)
      	to see, during that month, whether they have enough money to pay for something.  Suggested Savings is taken out of that and set
      	aside in another account to pay for long term items that may not fit in a single month.
     A report could be made after the comparison that itemizes the errors (unrecognized categories in pieChart.csv a sign that there are
       uncategorized items, or items that weren't categorized properly), or missing Categories in
       pieChart.csv.  (Something money wasn't spent on) which should be a normal occurence, but alerting may be useful.

       DollarsPerMonth.csv should be set at budget creation time (and hence set in stone).
       pieChart.csv is generated each time the program is run with the script.

       The algorithm for this would be as follows:
       read both pieChart.csv and DollarsPerMonth.csv into memory.  (If they're not already in memory).
       The memory structure of DollarsPerMonth.csv will have a slot for remembering that it was used for comparison in addition
       	   to the category and amount.
       for each item in: the pieChart map
       	   look for a similarly named item (ALL CAPS CATEGORIES) in DollarsPerMonth memory structure (map with usedForComparison addition)
	   if one if found:
	      subtract the amount of pieChart from DollarsPerMonth (DollarsPerMonth.category.value - pieChart.category.value)
	      to get a POSITIVE value of what is left to SPEND in that category.
	      (If the value is NEGATIVE the budget was overspent and not obeyed)
	      Mark the DollarsPerMonth category as USED.
	For the report:
	    print out all UNUSED categories in DollarsPerMonth as nothing spent in this category
	    print out all UNRECOGNIZED categories in pieChart (actual spending) as errors.  (Everything should be recognized.)
- Similar idea as before.
  I need a program (or part of this program), that:
    given a specific date (10 days back, 20 days back, etc.)
    	  Can be specified with a date (not necessarily a number of days back)
    measures what has been spent and compares to the specified MONTHLY budget.
    Then, it gives a status on how much has been spent on what to
    	  guide the user on what to stop spending on.


- Put a notes column in the transaction for notes that help remember what the transaction was for.
   Sometimes, for example, with AMAZON MKTPLC transactions, I have to look up what the transaction
   was for to figure out how to categorize it.  Then, I go back and have to look it up again for
   some other reason, like, "What was that money for?"
   If I add a notes section, I can add notes to transactions and see the notes I gathered about the
   transaction for later.

   There's already a place for this in each specific account (Not BigView...).  Just do a .schema
   and list the columns for each table (not the extended table BigView...) and you'll see a place
   for comments.
   
- Create a program that queries Amazon's system/databases to figure out which
transactions were for what do something based on this information, like
automatically categorize them.  Or just list the description from Amazon so the user
can decide what to categorize them as.
    I may be able to get ChatGPT or some other AI to create a program to do this for me.  That would be super cool.
    I tried this with AI, it didn't work.  Chase, also, has a feature that has a link to the purchase, so
    this idea is really too little too late.

- Make DollarsPerDayExpenditures a monthly thing.  (just include monthly in there and
ignore it.  You can calculate one based on the other.  For example, if daily doesn't exist
use the monthly for the budget numbers.
    Monthly is really the standard for measuring.  DollarsPerDay is really difficult to deal with.

- Fix the fact that there was an exception when no data was available at the beginning of
    the month.  There were 2 places this exception happened.

-   Create a program to evaluate the monthly status of a previous month.
    This could be put in the overall program at the end maybe, or not.
    It could either operate on the previous month, and this month, or
    allow the user to select a month to evaluate (most likely anothe program).

    Unfortunately, the selection of a month would inhibit the flow of the program
    and force the user's input on a program that used to just run by itself.




-----

REJECTED IDEAS

-----
- Implement CapitalOne credit card download and addition to DB.
    I tried to implement this and failed.  There was a problem with sqlite3.  It wouldn't
    let me create a prepared statement.  The troubleshooting logs are under:
    /Users/jbyrne/Dropbox/eclipse-workspace/finance/problems/2025_06_14


